/** Display plan for pricing UI: profile + workspace so purchasers see correct section and Manage billing. */
export type PricingDisplayPlan = "free" | "pro" | "pro_plus" | "enterprise" | "platform_admin";
